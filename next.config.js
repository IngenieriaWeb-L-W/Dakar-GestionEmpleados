/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com', 'e7.pngegg.com', 'upload.wikimedia.org', 'images.unsplash.com'],
  },
}

module.exports = nextConfig
